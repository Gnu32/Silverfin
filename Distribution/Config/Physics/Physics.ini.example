[Physics]
    ;;
    ;; Choose one of the physics engines below

    ;; AuroraOpenDynamicsEngine is by some distance the most developed physics engine and is the main supported physics engine of Aurora
    Include-AuroraOpenDynamicsEngine = Config/Physics/AuroraOpenDynamicsEngine.ini 

    ;; ModifiedBulletSim is an alternate physics engine that is not as developed and has plenty of bugs still to work out.
    ;Include-ModifiedBulletSim = Config/Physics/BulletPhysics.ini 

    ;; The most basic of all BasicPhysics
    ;DefaultPhysicsEngine = basicphysics
